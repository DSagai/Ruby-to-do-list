<%= form_for [@list, @task], :html => { :class => "form-horizontal task" } do |f| %>

  <% if @task.errors.any? %>
    <div id="error_expl" class="panel panel-danger">
      <div class="panel-heading">
        <h3 class="panel-title"><%= pluralize(@task.errors.count, "error") %> prohibited this task from being saved:</h3>
      </div>
      <div class="panel-body">
        <ul>
        <% @task.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
        </ul>
      </div>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :task_body, :class => 'control-label col-lg-2' %>
    <div class="col-lg-10">
      <%= f.text_field :task_body, :class => 'form-control' %>
    </div>
    <%=f.error_span(:task_body) %>
  </div>
  <div class="form-group">
    <%= f.label :done, :class => 'control-label col-lg-2' %>
    <div class="col-lg-10">
      <%= f.check_box :done, :class => 'form-control' %>
    </div>
    <%=f.error_span(:done) %>
  </div>

  <div class="form-group">
    <div class="col-lg-offset-2 col-lg-10">
      <%= f.submit nil, :class => 'btn btn-primary' %>
      <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                list_path(@list), :class => 'btn btn-default' %>
    </div>
  </div>

<% end %>
